import * as actions from '../actions';

const initialState = {
  events: [{
    'party-name': 'Create your own party',
    'event-summary': 'This is the most wonderful time of the year.',
    'party-type': 'Thanksgiving',
    'party-guests': 5
  }]
};

export const eventReducer = (state = initialState, action) => {

}